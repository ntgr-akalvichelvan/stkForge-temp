- name: Render interface configuration
  ansible.builtin.template:
    src: interface.j2
    dest: /tmp/{{ inventory_hostname }}_interface.cli
    mode: "0600"
  when: is_stack_master | default(true)

- name: Apply interface configuration
  ansible.builtin.shell: |
    sshpass -p "{{ ansible_password }}" \
    ssh -T \
        -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        {{ ansible_user }}@{{ ansible_host }} \
        < /tmp/{{ inventory_hostname }}_interface.cli
  when: is_stack_master | default(true)

